# 项目概述：甲状腺超声 AI 的跨数据集域漂移与结构化特征融合

## 一句话简介

基于四个公开数据集（TN5000、TN3K、Thy-Wise、ThyroidXL），量化甲状腺超声结节深度学习分类模型的跨数据集域漂移：检验联合多数据集训练能否恢复性能差距、增益能否迁移到完全未参与训练的队列，并验证"图像特征 + ACR TI-RADS 等结构化特征"融合的增量价值。

## 研究背景与要回答的问题

甲状腺结节非常常见，超声是首选检查，但判读主观、依赖操作者。已有深度学习模型大多在单中心、单数据集上报告很高的内部准确率，存在三个普遍问题：

1. **缺少外部验证**：单中心内部 AUC 会高估真实世界性能（跨医院、跨设备的域漂移）；
2. **丢弃临床信息**：放射科医生常规使用的 ACR TI-RADS 评分、结节尺寸、年龄、性别等结构化特征很少被融合；
3. **报告不完整**：多数工作不报告校准（ECE）和决策曲线（DCA）。

本研究用四个公开数据集直接回答：域漂移有多大？联合训练能否补回？加结构化临床特征是否值得？

## 研究设计

### Track A：跨数据集域漂移与联合训练

- 用 TN5000（5,000 张，活检确诊）训练纯图像模型（EfficientNetV2-S）；
- 在 TN3K（3,493 张）和 Thy-Wise（3,954 个结节 / 29,070 张）上做外部验证；
- 把 TN3K 训练子集加入训练池做"联合训练"，对比单数据集模型。

### Track B：三臂多模态消融（ThyroidXL）

- 图像分支：EfficientNetV2-S 编码器（512 维嵌入）；
- 临床分支：MLP 编码 5 个结构化特征（ACR TI-RADS 总分、结节宽、结节高、年龄、性别，64 维嵌入）；
- 三臂对比：图像-only / 临床-only / 融合（拼接后接 256→128 隐藏层，dropout 0.3）。

## 数据

| 数据集 | 规模 | 用途 | 许可 |
|---|---|---|---|
| TN5000 | 5,000 张图像 | 主训练 + 内部测试（70/15/15 分组划分） | 开放 |
| TN3K | 3,493 张图像（2,421 例患者） | 外部验证 #1 + 联合训练子集 | 开放 |
| Thy-Wise | 3,954 结节 / 29,070 张 | 外部验证 #2（结节级聚合） | CC BY 4.0 |
| ThyroidXL | 11,635 张 / 4,093 结节 | 融合消融（官方 dev/test 划分） | 门控（研究用途） |

所有划分均为患者级分组、无泄漏；TN5000↔TN3K 与 Thy-Wise↔(TN5000+TN3K) 均完成逐字节 MD5 查重（0 重复），报告见 paper/output/repro/tn5000_tn3k_dedup.json 与 thywise_dedup.json。

## 训练与评估协议

- 图像 224×224，随机 ShiftScaleRotate + 翻转增强；AdamW（初始学习率 1e-4，主干 1e-5，权重衰减 1e-4）+ 余弦退火；30 epochs；混合精度；RTX 3060 6GB；
- 最终协议统一 `pos_weight = 1.0`；主要结果用种子 42 锚点，稳健性用种子 123/2024 重训；
- 指标：AUC（bootstrap 2,000 次 95% CI）、固定 0.5 阈值下的灵敏度/特异度/准确率、ECE（10 等宽 bins）、Brier、决策曲线（阈值 0.05–0.95）；Thy-Wise/ThyroidXL 按结节内概率均值聚合；
- 报告规范：STARD 2015 + TRIPOD+AI 自查清单。

## 核心结果（最终协议，全部可复现）

### Track A：域漂移真实存在，联合训练恢复约一半差距

| 场景 | 单数据集模型 | 联合训练 | Δ |
|---|---|---|---|
| TN5000 内部测试（AUC） | 0.915 | 0.931 | +0.016 |
| TN3K 官方测试（AUC） | 0.729 | 0.813 | +0.085 |
| Thy-Wise 结节级（AUC） | 0.608 | 0.710 | +0.102 |

内部 0.915 → 外部 TN3K 全队列 0.712，量化出约 **0.20 的跨数据集性能落差**，单中心评估完全看不到。

### Track B：临床特征是补充信号，图像特征仍占主导

| 模型（结节级） | AUC (95% CI) | AUPRC | 灵敏度 | 特异度 | ECE |
|---|---|---|---|---|---|
| 图像-only | 0.939 (0.924–0.954) | 0.930 | 0.779 | 0.909 | 0.087 |
| 临床-only | 0.814 (0.782–0.847) | 0.735 | 0.00 | 1.00 | 0.248 |
| 融合 | 0.947 (0.932–0.960) | 0.939 | 0.725 | 0.940 | 0.118 |

- 融合 vs 图像：配对 bootstrap ΔAUC **+0.007（95% CI 0.002–0.013，p = 0.012）**；
- 三种子（42/123/2024，统一 pw1.0）集成 AUC **0.951**（0.937–0.964）；
- 亚组分析：融合增益集中在低中危 TI-RADS（TR2-3，Δ+0.046）与小结节（≤10 mm，Δ+0.014）；
- 专家 ACR TI-RADS 总分单独作为临床基线：AUC 0.929，说明深度模型提取了评分之外的图像信息。

## 可复现性（本项目最硬的优势）

- [`REPRODUCIBILITY.md`](REPRODUCIBILITY.md)：论文每个数字 → 复现命令 → 结果文件；
- [`paper/output/repro/`](paper/output/repro/README.md)：24 个规范评估 JSON 已入库（表 2/3、S1a–c、S2、S3、集成 CI、配对检验）；
- `verify_supplement_consistency.py` 一键核对论文与补充材料，输出 **PASS**；
- checkpoint 发布清单（11 个最终权重，含 SHA-256）见 `paper/output/repro/checkpoint_release_manifest.md`；
- 数据均为公开数据集，按文档下载即可端到端重跑。

## 仓库结构

```
train_thyroid.py / eval_*.py / prepare_*.py   训练、评估、数据准备
models/                                       EfficientNetV2-S + 融合分类器
run_*.sh                                      端到端复现脚本
paper/                                        手稿（md + Word）、图、复现结果
REPRODUCIBILITY.md                            数字→命令→结果文件 对照表
archive/                                      旧协议产物（本地留底，git-ignore）
```

## 当前状态

- 论文投稿准备中（目标 BMC Medical Imaging），代码与复现材料已整理为可公开发布状态；
- 最终协议产物（pw1.0、无泄漏划分、统一评估协议）为仓库唯一内容；
- 旧协议（leaky 划分 / pw0.35）痕迹已全部隔离，确保公开仓库无实验诚信风险。
